{# templates/cliente/novo.html.twig #}
{% extends 'forms/_form_base.html.twig' %}

{% block form_page_title %}Novo Cliente{% endblock %}
{% block page_title %}
    Novo Cliente
{% endblock %}

{% block btn_voltar %}
    <a class="me-md-5 mt-sm-3 btn btn-primary btm-sm mb-3 float-md-end" href="{{ url('cliente_index') }}"><i
                class="bi bi-arrow-left me-1"></i>Voltar</a>
{% endblock %}

{% block breadcrumb_items %}
    <li class="breadcrumb-item"><a href="#" class="text-decoration-none text-primary">Atendimento</a></li>
    <li class="breadcrumb-item"><a href="#" class="text-decoration-none text-primary">Clientes</a></li>
    <li class="breadcrumb-item active">Novo cliente</li>
{% endblock %}


{% block form_content %}
    <form method="post" action="{{ path('cliente_novo') }}">
        <div class="row">
            <div class="col-8"></div>
            <div class="col-4"></div>
        </div>
        <div class="row">
            <div class="col-md-8 mb-3">
                <label for="nome" class="form-label">Nome</label>
                <input type="text" class="form-control" id="nome" name="nome" required>
            </div>
            <div class="col-md-4 mb-3">
                <label for="cpf" class="form-label">CPF</label>
                <input type="text" class="form-control" id="cpf" name="cpf" placeholder="000.000.000-00">
            </div>
        </div>
        <div class="row">
            <div class="col-md-6 mb-3">
                <label for="email" class="form-label">Email</label>
                <input type="email" class="form-control" id="email" name="email">
            </div>
            <div class="col-md-3 mb-3">
                <label for="telefone" class="form-label">Telefone</label>
                <input type="text" class="form-control" id="telefone" name="telefone">
            </div>
            <div class="col-md-3 mb-3">
                <label class="form-label">Whatsapp?</label>
                <div class="btn-group w-100" role="group">
                    <input type="radio" class="btn-check" name="whatsapp" id="whatsapp1" autocomplete="off" checked>
                    <label class="btn btn-outline-primary" for="whatsapp1">Sim</label>
                    <input type="radio" class="btn-check" name="whatsapp" id="whatsapp2" autocomplete="off">
                    <label class="btn btn-outline-primary" for="whatsapp2">Não</label>
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col-md-2 mb-3">
                <label for="cep" class="form-label">CEP</label>
                <input type="text" class="form-control" id="cep" name="cep">
            </div>
            <div class="col-md-6 mb-3">
                <label for="rua" class="form-label">Rua</label>
                <input type="text" class="form-control" id="rua" name="rua">
            </div>
            <div class="col-md-1 mb-3">
                <label for="numero" class="form-label">Número</label>
                <input type="text" class="form-control" id="numero" name="numero">
            </div>
            <div class="col-md-3 mb-3">
                <label for="complemento" class="form-label">Complemento</label>
                <input type="text" class="form-control" id="complemento" name="complemento">
            </div>
        </div>
        <div class="row">
            <div class="col-md-6 mb-3">
                <label for="bairro" class="form-label">Bairro</label>
                <input type="text" class="form-control" id="bairro" name="bairro">
            </div>
            <div class="col-md-6 mb-3">
                <label for="cidade" class="form-label">Cidade</label>
                <input type="text" class="form-control" id="cidade" name="cidade">
            </div>
        </div>
        <div class="text-center">
            <button type="submit" class="btn btn-success w-50">Salvar</button>
        </div>
    </form>
{% endblock %}

{% block form_javascripts %}
    <script>
        $('.search-bar').addClass('d-none');
        $(function () {
            // Máscara de CPF
            $('#cpf').on('input', function () {
                let value = $(this).val().replace(/\D/g, '');
                if (value.length <= 11) {
                    value = value.replace(/(\d{3})(\d)/, '$1.$2');
                    value = value.replace(/(\d{3})(\d)/, '$1.$2');
                    value = value.replace(/(\d{3})(\d{1,2})$/, '$1-$2');
                    $(this).val(value);
                }
            });

            // CEP
            $('#cep').change(function () {
                var cep = $(this).val().replace('-', '').replace('.', '');
                if (cep.length === 8) {
                    $.get("https://viacep.com.br/ws/" + cep + "/json", function (data) {
                        if (!data.erro) {
                            $('#bairro').val(data.bairro);
                            $('#complemento').val(data.complemento);
                            $('#cidade').val(data.localidade);
                            $('#rua').val(data.logradouro);
                        }
                    }, 'json');
                }
            });
        });
    </script>
{% endblock %}