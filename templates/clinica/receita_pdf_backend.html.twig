<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <style>
        body { font-family: DejaVu Sans, sans-serif; }
        .receita-container { padding: 20px; border: 1px solid #ccc; border-radius: 12px; }
        .cabecalho, .rodape { text-align: center; margin-bottom: 30px; }
        .conteudo { margin: 40px 0; }
    </style>
</head>
<body>
    <div class="receita-container">
        <div class="cabecalho">{{ cabecalho|raw }}</div>
        <div class="conteudo">{{ conteudo|raw }}</div>
        <div class="rodape">{{ rodape|raw }}</div>
    </div>
</body>
</html>
